# Copy this file to .env and point it at the running backend.
VITE_API_BASE_URL=http://localhost:5000/api

# IMPORTANT: if this URL changes (e.g. deployed backend, different port), also update
# the connect-src directive in index.html's Content-Security-Policy meta tag, or the
# browser will silently block every API request.
